"""
demoapp.py REMOVED — this file contained test/demo routes.

The demo/test code was intentionally removed to keep the project focused on the main `app.py`.

If you need the demo functionality later, restore from source control or recreate a small test script.
"""